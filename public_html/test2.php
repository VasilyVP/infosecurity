<?php
 /*
    use GuzzleHttp\Client;
    // загружаем пререквизиты
    require_once $_SERVER['DOCUMENT_ROOT'] . '/mandatory.php';

    $client = new Client([
        'base_uri' => "http://localhost:3000/render?url=https://ya.ru", //$reqUrl
        'verify' => false
    ]);

    $response = $client->get('');

    echo $response->getBody();
*/
    //echo date(DATE_RFC822);


    phpinfo();